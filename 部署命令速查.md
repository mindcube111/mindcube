# ⚡ 部署命令速查表

## 🚀 最常用的部署命令

### 一键部署（推荐首次使用）
```bash
npm run deploy
```

### 快速部署（需要已安装 Wrangler）
```bash
npm run deploy:quick
```

### 手动部署（分步执行）
```bash
# 1. 构建
npm run build

# 2. 部署
wrangler pages deploy dist --project-name=psychological-assessment-platform
```

## 📦 所有可用命令

| 命令 | 说明 | 适用场景 |
|------|------|----------|
| `npm run deploy` | 完整部署流程（交互式） | 首次部署或需要检查环境 |
| `npm run deploy:ps1` | PowerShell 部署脚本 | Windows 系统 |
| `npm run deploy:quick` | 快速部署 | 已安装 Wrangler，快速更新 |
| `npm run deploy:cloudflare` | Cloudflare 部署 | 同 deploy:quick |
| `npm run deploy:build` | 仅构建 | 只构建不部署 |
| `npm run build` | 构建项目 | 标准构建命令 |

## 🔧 前置命令

### 安装 Wrangler（首次需要）
```bash
npm install -g wrangler
```

### 登录 Cloudflare（首次需要）
```bash
wrangler login
```

## ✅ 部署后验证

```bash
# 访问网站
https://psychological-assessment-platform.pages.dev

# 测试 API
https://psychological-assessment-platform.pages.dev/api/questionnaires/available
```

---

**快速开始：** 运行 `npm run deploy` 即可！











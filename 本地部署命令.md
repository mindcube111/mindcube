# 🚀 本地部署命令速查

## 快速部署

### 方式一：使用批处理文件（Windows - 最简单）

```bash
# 双击运行或命令行执行
deploy.bat
```

### 方式二：使用 PowerShell 脚本（Windows）

```powershell
# 方式 A：使用 npm 脚本
npm run deploy:ps1

# 方式 B：直接运行脚本
.\scripts\deploy.ps1

# 方式 C：使用 PowerShell 执行
powershell -ExecutionPolicy Bypass -File .\scripts\deploy.ps1
```

### 方式三：使用 Node.js 脚本（跨平台）

```bash
npm run deploy
```

## 分步部署命令

### 步骤 1: 安装依赖（如果需要）

```bash
npm install
```

### 步骤 2: 构建项目

```bash
npm run build
```

这会执行：
- ✅ Vite 构建（打包前端代码）
- ✅ 复制 Functions 文件到 `dist/functions`

### 步骤 3: 验证构建产物

检查 `dist` 目录是否包含：
- ✅ `dist/index.html`
- ✅ `dist/assets/` (JS/CSS 文件)
- ✅ `dist/functions/_middleware.js`
- ✅ `dist/functions/api/[[path]].js`

### 步骤 4: 部署到 Cloudflare Pages

#### 选项 A：使用 Wrangler CLI（推荐）

```bash
# 1. 安装 Wrangler（如果还没有）
npm install -g wrangler

# 2. 登录 Cloudflare（首次需要）
wrangler login

# 3. 部署
wrangler pages deploy dist --project-name=psychological-assessment-platform
```

#### 选项 B：使用 npm 脚本

```bash
# 一键构建并部署
npm run deploy:quick

# 或
npm run deploy:cloudflare
```

#### 选项 C：手动上传

1. 访问 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **Pages** > **Create a project**
3. 选择 **Upload assets**
4. 上传 `dist` 目录

## 快速命令参考

```bash
# 开发环境
npm run dev

# 构建项目
npm run build

# 预览构建结果
npm run preview

# 完整部署流程
npm run deploy              # 交互式部署脚本
npm run deploy:ps1          # PowerShell 部署脚本
npm run deploy:quick        # 快速部署（需要已安装 wrangler）

# 仅构建（不部署）
npm run build
```

## 部署前检查清单

- [ ] Node.js 16+ 已安装
- [ ] 依赖已安装 (`npm install`)
- [ ] 构建成功 (`npm run build`)
- [ ] `dist` 目录包含必要文件
- [ ] Wrangler CLI 已安装（如果使用 CLI 部署）
- [ ] 已登录 Cloudflare（如果使用 CLI 部署）

## 项目信息

- **项目名称**: `psychological-assessment-platform`
- **构建输出目录**: `dist`
- **构建命令**: `npm run build`
- **Functions 目录**: `dist/functions`

## 常见问题

### 构建失败

```bash
# 清理并重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 清理构建目录并重新构建
rm -rf dist
npm run build
```

### Wrangler 未安装

```bash
npm install -g wrangler
wrangler --version
```

### 未登录 Cloudflare

```bash
wrangler login
```

### 查看部署状态

```bash
wrangler pages deployment list --project-name=psychological-assessment-platform
```

## 部署后验证

部署成功后，访问：

- **网站地址**: `https://psychological-assessment-platform.pages.dev`
- **API 测试**: `https://psychological-assessment-platform.pages.dev/api/questionnaires/available`








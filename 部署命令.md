# 🚀 部署命令速查

## 快速部署（推荐）

### 方式一：一键部署脚本

```bash
# Windows - 双击运行
deploy.bat

# 或命令行执行
.\deploy.bat
```

### 方式二：使用 npm 脚本

```bash
# 交互式部署（推荐首次使用）
npm run deploy

# 快速部署（需要已安装 wrangler）
npm run deploy:quick

# PowerShell 脚本部署
npm run deploy:ps1
```

## 分步部署

### 1. 构建项目

```bash
npm run build
```

### 2. 部署到 Cloudflare Pages

```bash
wrangler pages deploy dist --project-name=psychological-assessment-platform
```

### 一键构建并部署

```bash
npm run deploy:cloudflare
```

## 首次部署准备

### 1. 安装 Wrangler CLI

```bash
npm install -g wrangler
```

### 2. 登录 Cloudflare

```bash
wrangler login
```

这会打开浏览器，完成 Cloudflare 账号登录。

## 完整部署流程

```bash
# 1. 确保依赖已安装
npm install

# 2. 构建项目
npm run build

# 3. 部署（已安装 wrangler 的情况下）
wrangler pages deploy dist --project-name=psychological-assessment-platform
```

## 命令说明

| 命令 | 说明 |
|------|------|
| `npm run build` | 构建项目，生成 `dist` 目录 |
| `npm run deploy` | 交互式部署脚本（跨平台） |
| `npm run deploy:ps1` | PowerShell 部署脚本 |
| `npm run deploy:quick` | 快速部署（构建+部署） |
| `npm run deploy:cloudflare` | 构建并部署到 Cloudflare |
| `wrangler pages deploy dist` | 使用 Wrangler 部署 |

## 部署后验证

部署成功后，访问：

- **网站地址**: https://psychological-assessment-platform.pages.dev
- **API 测试**: https://psychological-assessment-platform.pages.dev/api/questionnaires/available

## 查看部署状态

```bash
wrangler pages deployment list --project-name=psychological-assessment-platform
```

## 常见问题

### 构建失败
```bash
# 清理并重新安装
rm -rf node_modules dist
npm install
npm run build
```

### 未登录 Cloudflare
```bash
wrangler login
```

### Wrangler 未安装
```bash
npm install -g wrangler
```








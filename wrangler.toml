# Cloudflare Pages 配置文件
# 用于配置 Pages 项目的构建设置

name = "psychological-assessment-platform"
compatibility_date = "2024-01-01"

# Pages 构建输出目录
pages_build_output_dir = "dist"

# KV Namespace 绑定（如果需要）
# [[kv_namespaces]]
# binding = "KV_STORE"
# id = "your-kv-namespace-id"

# 环境变量（生产环境）
# [vars]
# NODE_ENV = "production"

# ⚡ 快速部署指南

## 🚀 一键部署命令

### Windows (PowerShell)

```powershell
# 1. 进入项目目录
cd "02 管理器"

# 2. 安装依赖（如果还没有）
npm install

# 3. 构建项目
npm run build

# 4. 部署到 Cloudflare Pages
# 方式 A: 通过 Wrangler CLI（需要先安装 wrangler）
npm install -g wrangler
wrangler login
wrangler pages deploy dist --project-name=psychological-assessment-platform

# 方式 B: 通过 Cloudflare Dashboard
# - 访问 https://dash.cloudflare.com/
# - 进入 Pages > Create a project
# - 上传 dist 目录
```

## 📋 部署步骤详解

### 步骤 1: 构建项目

```bash
npm run build
```

这个命令会：
- ✅ 运行 TypeScript 类型检查
- ✅ 使用 Vite 打包项目
- ✅ 复制 Functions 文件到 dist 目录

### 步骤 2: 验证构建产物

检查 `dist` 目录是否包含：
- ✅ `dist/index.html`
- ✅ `dist/assets/` (JS/CSS 文件)
- ✅ `dist/functions/_middleware.js`
- ✅ `dist/functions/api/[[path]].js`

### 步骤 3: 部署到 Cloudflare

#### 选项 A: 使用 Wrangler CLI（推荐）

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 部署
wrangler pages deploy dist --project-name=您的项目名称
```

#### 选项 B: 使用 Cloudflare Dashboard

1. 访问 https://dash.cloudflare.com/
2. 点击左侧菜单 "Pages"
3. 点击 "Create a project"
4. 选择 "Upload assets"
5. 拖拽或选择 `dist` 目录上传
6. 点击 "Deploy site"

#### 选项 C: 连接 Git 仓库（推荐用于持续部署）

1. 在 Cloudflare Dashboard 中创建 Pages 项目
2. 选择 "Connect to Git"
3. 授权并选择您的仓库
4. 配置构建设置：
   - Build command: `npm run build`
   - Build output directory: `dist`
   - Root directory: `02 管理器` (如果项目在子目录)
5. 每次推送代码会自动部署

## ✅ 部署后验证

部署完成后，访问：

- 网站地址: `https://[您的项目名].pages.dev`
- API 测试: `https://[您的项目名].pages.dev/api/questionnaires/available`

## 🛠️ 常见问题

### Q: 构建失败怎么办？
A: 
1. 确保所有依赖已安装: `npm install`
2. 检查 Node.js 版本（推荐 18+）
3. 查看构建错误信息并修复

### Q: Functions 不工作？
A:
1. 确认 `dist/functions` 目录存在
2. 检查构建脚本是否包含 `copy-functions`
3. 在 Cloudflare Dashboard 中查看 Functions 状态

### Q: 网站显示 404？
A:
1. 等待 2-5 分钟让域名生效
2. 清除浏览器缓存
3. 检查部署状态

## 📝 环境变量配置

如果项目需要环境变量：

1. 在 Cloudflare Dashboard 中进入项目设置
2. 选择 "Environment variables"
3. 添加所需的变量
4. 重新部署

## 🔄 更新部署

每次更新代码后：

```bash
# 重新构建
npm run build

# 重新部署
wrangler pages deploy dist --project-name=您的项目名称
```

或直接推送代码到 Git（如果已连接）：

```bash
git add .
git commit -m "更新内容"
git push
# Cloudflare 会自动部署
```

---

**需要帮助？** 查看 [完整部署指南](部署指南.md)

